spring.application.name=ApiGateway
server.port=8082

#Note that event the spring cloud api gateway is registered as an eureka client.
eureka.client.service-url.defaultZone=http://localhost:8761/eureka